from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='One place to create model, validate and deploy',
    author='Harsha',
    license='',
)
